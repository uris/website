export enum ToolNamespace {
	projects = 'projects',
	ui = 'ui',
}

export enum ToolType {
	RequestProjectDetails = 'request_project_details',
	OpenProjectView = 'open_project_view_in_browser',
	UpdateUiSettings = 'update_ui_settings',
}

export enum UIAction {
	Theme = 'theme',
	Volume = 'volume',
}
